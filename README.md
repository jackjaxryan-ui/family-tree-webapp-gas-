# family-tree-webapp-gas-
# Family Tree Web App (Google Apps Script)  A lightweight family tree data intake application built with Google Apps Script and Google Sheets as the backend database.  The system allows users to input family relationships (parents, spouse, children) and automatically maintains the relational structure inside Google Sheets.
